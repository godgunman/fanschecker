<!DOCTYPE html>
<html>
    <meta charset="utf-8">
    <style>

        body {
            font: 10px sans-serif;
        }

        .axis path,
        .axis line {
            fill: none;
            stroke: #000;
            shape-rendering: crispEdges;
        }

        .area {
            fill: steelblue;
        }

    </style>
    <head>
        <script src="http://d3js.org/d3.v3.js"></script>
        <script src="//cdnjs.cloudflare.com/ajax/libs/jquery/2.1.0/jquery.min.js"></script>
        <script src="//cdnjs.cloudflare.com/ajax/libs/semantic-ui/0.13.0/javascript/semantic.min.js"></script>
        <link rel="stylesheet" type="text/css" href="//cdnjs.cloudflare.com/ajax/libs/semantic-ui/0.13.0/css/semantic.css">
    </head>
    <body>
        <script>

            var margin = {top: 20, right: 20, bottom: 30, left: 50},
            width = 960 - margin.left - margin.right,
            height = 700 - margin.top - margin.bottom;

            var parseDate = d3.time.format("%d-%b-%y").parse;

            var x = d3.time.scale()
            .range([0, width]);

            var y = d3.scale.linear()
            .range([height, 0]);

            var xAxis = d3.svg.axis()
            .scale(x)
            .orient("bottom");

            var yAxis = d3.svg.axis()
            .scale(y)
            .orient("left");

            var area = d3.svg.area()
            .x(function(d) { return x(new Date(d.createdAt)); })
            .y0(height)
            .y1(function(d) { return y(d.likes); });

            var svg = d3.select("body").append("svg")
            .attr("width", width + margin.left + margin.right)
            .attr("height", height + margin.top + margin.bottom)
            .append("g")
            .attr("transform", "translate(" + margin.left + "," + margin.top + ")");

            $(function() {
                $.get('/api/fans?limit=5000', function(data) {
                    data = data.result;
                    console.log(data);

                    x.domain(d3.extent(data, function(d) { return new Date(d.createdAt); }));
                    y.domain(d3.extent(data, function(d) { return +d.likes; }));

                    svg.append("path")
                    .datum(data)
                    .attr("class", "area")
                    .attr("d", area);

                    svg.append("g")
                    .attr("class", "x axis")
                    .attr("transform", "translate(0," + height + ")")
                    .call(xAxis);

                    svg.append("g")
                    .attr("class", "y axis")
                    .call(yAxis)
                    .append("text")
                    .attr("transform", "rotate(-90)")
                    .attr("y", 6)
                    .attr("dy", ".71em")
                    .style("text-anchor", "end")
                    .text("Price ($)");
                    $('body').append($('<h3>').append('Start: ' + data[data.length-1].createdAt));
                    $('body').append($('<h3>').append('~'));
                    $('body').append($('<h3>').append('End: ' + data[0].createdAt));
                    $('.ui.page.dimmer').dimmer('hide');
                });
            });

        </script>
        <script>
            $(function(){
                $('.ui.page.dimmer').dimmer('show');
            });
        </script>
        <div class="ui page dimmer">
            <div class="content">
                <div class="center"><i class="massive loading icon"></i> Loading</div>
            </div>
        </div>
    </body>
    <script>
        (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
            (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
            m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
        })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

        ga('create', 'UA-49959792-1', 'fanschecker.herokuapp.com');
        ga('send', 'pageview');

    </script>
</html>
